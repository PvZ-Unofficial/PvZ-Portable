# PvZ-Portable 项目约束

## 当前目标

本工作区中的 PvZ-Portable 是需要长期维护的派生项目。当前改造目标是：保留
现有项目中保证游戏可玩、跨平台运行、资源加载、构建、打包和开发工具可用的
修改，同时把游戏逻辑还原到 PvZ 1.0.0.1051。

- 不要整文件或整目录覆盖为 1051 反编译源码；先分清游戏逻辑、年度版内容、
  bug 修复与可移植性改造，再做函数级修改。
- 默认保留渲染、音频、输入、文件系统、字符编码、平台适配、CMake、CI、打包
  和工具链修改，除非它们直接改变了游戏行为。
- 修改看似奇怪的代码或注释前，先用 `git blame`、`git log -S` 或
  `git log -L` 查明它来自原始反编译、年度版还原还是后续移植。
- 对相邻仓库的修改必须分别检查状态、分别提交；不得把多个仓库混入一个 Git
  操作。

## 版本称谓

- **原版**：Plants vs. Zombies 1.0.0.1051。
- **年度版**：PvZ-Portable 当前还原的 Plants vs. Zombies 1.2.0.x；当前主要
  参考版本是 1.2.0.1073。

这里的“还原到原版”专指游戏规则、状态机、时序、常量和可观察行为；不表示
撤销为了可玩性、跨平台构建或工具支持所做的工程改造。

## 还原验收边界

- 以 1.0.0.1051 的实际可观察行为为目标，包括原版自身已经确认存在的良性
  bug。上界之风、双向豌豆等机制必须忠实保留，不得被 `DO_FIX_BUGS`、通用
  “修 bug”清理或年度版行为覆盖。此类行为属于还原目标，不归类为
  PvZ-Portable 自身 bug。
- 若原版行为依赖可能导致 fatal error、崩溃或内存破坏的未定义行为，可以改成
  定义明确的安全实现；应尽量保留其非致命路径上的可观察行为，并在对应记录中
  写明原版证据、风险和有意偏离之处。
- 移除年度版独有的成就、僵尸形象编辑器（Zombatar）及其专属界面、游戏逻辑
  和资源依赖。迁移可以分批完成，但这些内容不属于需要保留的可玩性或跨平台
  改造。
- 最终程序必须能够直接使用 1.0.0.1051 原版资源运行，不得要求年度版
  `main.pak` 或年度版独有资源。可以保留 Portable 的资源加载和路径适配代码，
  但其默认资源名称、布局和缺失资源处理必须兼容 1051 原版资源。

## 源码沿革

本项目和 1051 反编译项目没有 GitHub fork 关系，但存在连续的源码沿革：

1. `ruslan831/PlantsVsZombies-decompilation` 的源码树曾被标成
   `0.9.9.1029`；本工作区按已经确认的结论将其视为 1.0.0.1051 源码。
2. Patoke 的 `re-plants-vs-zombies` 在这份源码基础上继续还原年度版，并留下
   大量 `@Patoke` 标记和年度版相关修改。
3. Headshot2017 在该项目上继续进行跨平台移植。
4. PvZ-Portable 沿用这条代码线并继续独立开发。当前仓库保留了上述历史的
   Git 提交，例如 Patoke 的初始提交 `42be5bdde899b26ed666ed4652df341d1d82c57f`、
   合并 Patoke 仓库的 `e90839a63c26430607b2d9c6c83c5e75701e051f`，以及后续
   Headshot2017 的提交；但 `wszqkzqk/PvZ-Portable` 在 GitHub 上是独立仓库，
   不是这些项目的 fork。

因此，奇怪的注释、命名或实现首先应被当作历史线索，不要未经核对就清理。
当前仓库自身的 Git 历史已经包含本次还原所需的中间演变记录；
`re-plants-vs-zombies` 目前不是必须安装到工作区的正式来源。

## 证据来源

### 原版

- 正式源码检出：`../PlantsVsZombies-decompilation/`。
- 反编译源码可能有错；涉及游戏行为时，以原版二进制的反汇编、反编译结果或
  真实运行验证为最终依据。
- 二进制：
  `C:\Users\123\Desktop\Plants_Vs_Zombies_V1.0.0\Plants_Vs_Zombies_V1.0.0.1051_EN\PlantsVsZombies.exe`
- SHA-256：`F9669AF338964787A3785A7895791297D599295B8BB669B0DB49443F736A1322`
- 已校正的反编译源码错误见
  [`已经修改的反汇编项目bug合集.md`](../PlantsVsZombies-decompilation/已经修改的反汇编项目bug合集.md)。

### 年度版

- 当前参考二进制：
  `C:\Users\123\Desktop\年度加强版精简版\PlantsVsZombies.exe`
- 文件版本：`1.2.0.1073`。
- SHA-256：`E1511201EC2AE9A7E30259F04BC2EC7272BADB2574A2E9B405D0E916AE5C082C`
- PvZ-Portable 当前源码和历史提交只能说明实现及修改意图；判定年度版真实行为
  时，仍应使用年度版二进制或真实运行结果复核。

历史提交用于定位差异来源，反编译源码用于理解候选逻辑，两个版本的二进制和
真实运行结果用于裁决行为。不要仅凭提交信息中的 “fix” 或注释中的版本判断
差异性质。

## 问题分类与迁移

| 记录 | 用途 | 完成后的去向 |
| --- | --- | --- |
| [`docs/可用于修改1051反汇编项目的历史修改`](docs/可用于修改1051反汇编项目的历史修改) | 历史修改表明 1051 反编译源码不等价于原版二进制，且应修正反编译项目 | 移到反编译项目的 [`已经修改的反汇编项目bug合集.md`](../PlantsVsZombies-decompilation/已经修改的反汇编项目bug合集.md) |
| [`docs/新发现的bug`](docs/新发现的bug) | PvZ-Portable 中即使相对年度版也存在、且历史修复中未找到的 bug | 移到 [`docs/已经修复的bug合集`](docs/已经修复的bug合集) |
| [`docs/已发现但尚未还原的与1051不同之处`](docs/已发现但尚未还原的与1051不同之处) | 年度版与原版的版本差异，尚未在 PvZ-Portable 中还原 | 移到 [`docs/已还原的与1051不同之处`](docs/已还原的与1051不同之处) |

分类前必须先回答：它是反编译源码错误、PvZ-Portable 自身 bug、年度版与原版的
真实差异，还是不影响游戏行为的移植/构建修改。证据不足时保留在未完成记录中
并明确写“待复核”，不得猜测性修代码。

每个未完成记录至少包含：

- 问题介绍和涉及的函数或文件；
- 原版、年度版或历史反编译中的简要证据；涉及边界、常量或分支方向时记录
  函数地址或关键指令；
- 一至两句话说明会造成的实际游戏行为差异；
- 当前状态和仍需确认的事项。

每个已完成记录还必须包含修正内容、修改文件和**实际修复提交的完整提交号**。
先提交代码修复，再在后续文档提交中记录该 hash；不要记录文档迁移提交自身的
hash。迁移时在同一次文档修改中从未完成记录删除并加入已完成记录，避免两边
重复保存同一问题。一个问题只选择一份未完成台账作为主记录；跨类别关系使用
链接或简短备注。

记录格式参考 `../PvZ-Emulator/pe模拟器问题合集.md` 和
`../PvZ-Emulator/已经修改的bug合集.md`。

## 修改与验证原则

1. 修改前查看三份未完成/已完成台账和相关历史提交，避免重复工作。
2. 对游戏逻辑差异分别核对原版与年度版；反编译源码和当前 Portable 实现均不
   能单独作为最终结论。
3. 修改应按可独立验证的函数或机制分批完成，避免把构建重构、格式化和逻辑
   还原混进同一提交。
4. 至少完成受影响构建配置的编译检查；会改变游戏行为的修改还要留下最小可
   重复验证，或在记录中写明人工复现步骤和观察结果。
5. 若发现 1051 反编译源码错误，需要修改相邻反编译仓库时，先单独确认该仓库
   工作区干净，并在其自己的分支和提交中完成修正。
